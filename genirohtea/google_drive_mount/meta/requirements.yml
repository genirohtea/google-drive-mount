---
collections:
  # Required for molecule testing
  # v3.10.3 or higher due to python requests issue
  - name: community.docker
    version: "3.12.1"
  # Provides the bitwarden_secrets_manager lookup (uses the `bws` CLI)
  - name: community.general
    version: ">=7.2.0"
roles:
  - name: stefangweichinger.ansible_rclone
    version: "0.2.6"
